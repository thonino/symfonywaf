{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>


<div class="container bg-secondary mt-2 pb-2">
    
    <h3 class="">{{ invoice.lastname }} {{ invoice.firstname }} </h3>
    <h5 class="text-light">{{ invoice.phone }}</h5>
    <h5 class="text-light">{{ invoice.address }}</h5>
    <h5 class="text-light">Prix tolal : {{ invoice.totalPrice}} € </h5>


{% endblock %}

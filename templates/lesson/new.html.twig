{% extends 'base.html.twig' %}

{% block title %}Nouveau cours{% endblock %}

{% block body %}
<div class="d-flex justify-content-center">
    <div class="container bg-secondary m-2 pb-2"style="border-radius:10px">
        <h1 class="scroll">Nouvelle cours</h1>
        {{ include('lesson/_form.html.twig') }}
        <a class="scroll btn btn-primary"href="{{ path('app_lesson_index') }}"> Retour </a>
    </div>
</div>
{% endblock %}

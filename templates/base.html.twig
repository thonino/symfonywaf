<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        {% block head %}{% endblock %}
        <title>{% block title %}WA Formation{% endblock %}</title>
{# Bootstrap & css // templates/base.html.twig #}
<link href="{{ asset('bootstrap.min.css')}}" rel="stylesheet">
<link href="{{ asset('css/styles.css')}}" rel="stylesheet"> 
<link href="{{ asset('bootstrapIcons/bootstrap-icons.css')}}" rel="stylesheet"> 
</head>
    <body style="background-color: #dfdfdf;;">
    <style>
    .header p{font-family: 'Poiret One', cursive;font-weight: bold;}
    a{font-family: 'Bad Script', cursive;}
    .tailleTxt{font-size: 50px;}
    .cookies{  width: 100%;height: 40px;bottom: 24px;}
    .ok{text-decoration:none;}
    /* Au niveau le plus haut du code */
</style>
{# NavBar#}
<nav class="navbar py-0 navbar-expand-lg navbar-dark bg-light ">
    <div class="container-fluid">
        <a class="navbar-brand fs-3" href="{{ path('app_home') }}"><p class="text-info d-inline "style="font-family: 'Poiret One', cursive;font-weight: bold;">W<span class="text-success">A</span><span class="text-dark">FORMATION</span></p></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="{{ path('app_lesson_index') }}">Leçons </a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="{{ path('app_calendar') }}">Calendrier</a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="{{ path('app_booking_index') }}">Reservations</a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="{{ path('app_invoices') }}">Factures</a>
            </li>
                            <li class="nav-item" >
                    <a class="nav-link active" aria-current="page" href="{{ path('cart_betaTest') }}">Test</a>
                </li>
            </ul>
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <li class="nav-item" >
                    <a class="nav-link active" aria-current="page" href="{{ path('cart_show') }}">
                        <i class="bi-cart text-white mx-1"> </i>Panier
                        
                    </a>
                </li>
            {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item" >
                    <a class="nav-link active" aria-current="page" href="{{ path('app_admin') }}">Admin</a>
                </li>
                                </li>
                        <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{{ path('app_logout') }}">Déconnexion</a>
                </li>
            
            {% elseif app.user and not is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="#">Compte</a>
                </li>
                        <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{{ path('app_logout') }}">Déconnexion</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{{ path('app_register') }}">S'inscrire</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{{ path('app_login') }}">Connexion</a>
                </li>
            {% endif %}
            </ul>
        </div>
    </div>
</nav>
<!-- FOOTER -->
<footer class="scroll text-center  bg-dark fixed-bottom">
    <div class="text-center ">
        <a class="text-light text-decoration-none mx-2" href="#">Conditions générales de vente</a>
        <a class="text-light text-decoration-none mx-2" href="#">Partenariats</a>
        <a class="text-success text-decoration-none mx-2 bold" href="#">Contact</a>
    </div>
</footer>
        {% block body %}{% endblock %}
        {# bootstrap & js #}
        <script src="{{ asset('bootstrap.bundle.min.js')}}" defer>
        </script><script src="{{ asset('js/index.js')}}" defer></script>
        <script src="https://unpkg.com/scrollreveal"></script>
    </body>
</html>
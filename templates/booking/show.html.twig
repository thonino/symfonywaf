{% extends 'base.html.twig' %}
{% block title %}Booking{% endblock %}
{% block body %}
<div class="container bg-secondary mt-2 pb-2">
    <h1>Réservation</h1>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ booking.id }}</td>
            </tr>
            <tr>
                <th>title</th>
                <td>{{ booking.title }}</td>
            </tr>
            <tr>
                <th>Start</th>
                <td>{{ booking.start ? booking.start|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>
    <a class="btn btn-success mb-2 " href="{{ path('app_booking_edit', {'id': booking.id}) }}">Modifier</a><br>
    {{ include('booking/_delete_form.html.twig') }}
    <a class="btn btn-primary mt-2 " href="{{ path('app_booking_index') }}">Retour</a>
</div>
    
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Modifier un cours{% endblock %}

{% block body %}
<div class="d-flex justify-content-center">
    <div class="scroll container bg-secondary m-2 pb-2"style="border-radius:10px">
        <h1 class="scroll">Modifier le cours</h1>
        {{ include('lesson/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        {{ include('lesson/_delete_form.html.twig') }}
        <a class="scroll btn btn-primary mt-2" href="{{ path('app_lesson_index') }}">Retour</a>
    </div>
</div>
{% endblock %}

{% extends 'base.html.twig' %}
{% block title %}Modifier Booking{% endblock %}
{% block body %}
    <div class="d-flex justify-content-center">
        <div class="scroll container bg-secondary m-2 pb-2"style="border-radius:10px">
            <h1 class="scroll">Modifier</h1>
            {{ include('booking/_form.html.twig', {'button_label': 'Update'}) }}
            <a class="scroll btn btn-primary  mb-2" href="{{ path('app_booking_index') }}">Retour</a>
            {{ include('booking/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}

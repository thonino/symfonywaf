{% extends 'base.html.twig' %}

{% block title %} panier{% endblock %}

{% block body %}
<div class="container  mt-3">
    <table class="table">
        <thead>
            <tr>
                <th class="fs-4 text-warning">Votre Panier</th>
                <th class="fs-4 text-warning">Prix</th>
                <th class="fs-4">Quantité</th>
            </tr>
        </thead>
        <tbody>
            {% for row in cartLessons %}
            <form method="post">
        <tr>
                <td  class="fs-5">{{ row.lesson.name }}  <span  class="text-light fs-6">{{ row.lesson.title }}</span><br>
                    <input class="m-2" type="text" placeholder="Prénom" name="title"/>
                    <input class="datetype" type="datetime-local" name="start"/></span>
                    {# {% for booking in bookings %}
                    {{ booking.id }}
                    {{ booking.title  }}
                    {{ booking.start ? booking.start|date('Y-m-d H:i:s') : '' }}
                    <a class="btn btn-sm btn-success " href="{{ path('app_booking_edit', {'id': booking.id}) }}">Modifier</a>
                    {% endfor %} #}
                </td>
            <td class="fs-4">{{ row.lesson.price }} €</td>
            <td class=" fs-4">
                <a href="{{ path('cart_less', {'lesson':row.lesson.id}) }}"><i class="bi bi-file-minus-fill fs-4 mx-2 text-danger"></i></a>
                {{ row.qty }} H
                <a href="{{ path('cart_add', {'lesson':row.lesson.id}) }}"><i class="bi bi-plus-square-fill fs-4 mx-2 text-warning"></i></a>
                <a href="{{ path('cart_del', {'lesson':row.lesson.id}) }}"><i class="bi bi-trash fs-4 mx-2 text-danger"></i></a></td>
        </tr>
        {% else %}
            <tr>
                <td colspan="2"> Aucun article ajouté </td>            
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {# // Booking Reservation #}
    {# <table class="table">
        <thead>
            <tr>
                <th class="fs-6 mx-2">Numero de réservation</th>
                <th class="fs-4">Nom </th>
                <th class="fs-4">Date du RDV</th>
                <th class="fs-4">Action</th>
            </tr>
        </thead>
        <tbody>
        {% for booking in bookings %}
            <tr>
                <td>{{ booking.id }}</td>
                <td>{{ booking.title  }}</td>
                <td>{{ booking.start ? booking.start|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <div class="d-flex">
                        <a class="btn btn-success mt-1" href="{{ path('app_booking_edit', {'id': booking.id}) }}">Modifier</a>
                        {{ include('booking/_delete_form.html.twig') }}
                    </div>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">Pas de reservation</td>
            </tr>
        {% endfor %}
        </tbody>
    </table> #}


        <button  type="submit"  class="btn btn-success mx-2"> 
            {# <a  class="text-white text-decoration-none" href="{{ path('cart_show',{'id': menu.id}) }}">Valider Panier </a> #}
            {# <a  class="text-white text-decoration-none" href="{{ path('cart_show' )}}"> #}
            Payer
            {# </a> #}
        </button>
        {# <button  type="submit" class="btn btn-success mx-2">Valider</button> #}
    </form>
    <p class="fs-4 border border-4 rounded px-2 text-warning"style="float:right"> Total : 
    <span class="text-white fs-4"> {{ total }} € </span></p>
</div>
{% endblock %}
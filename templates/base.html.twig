<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        {% block head %}{% endblock %}
        <title>{% block title %}WA Formation{% endblock %}</title>
{# Bootstrap & css // templates/base.html.twig #}
<link href="{{ asset('bootstrap.min.css')}}" rel="stylesheet">
<link href="{{ asset('css/styles.css')}}" rel="stylesheet"> 
<link href="{{ asset('bootstrapIcons/bootstrap-icons.css')}}" rel="stylesheet"> 
</head>
    <body style="background-color: #dfdfdf;;">
    <style>
    .header p{font-family: 'Poiret One', cursive;font-weight: bold;}
    a{font-family: 'Bad Script', cursive;}
    .tailleTxt{font-size: 50px;}
    .cookies{  width: 100%;height: 40px;bottom: 24px;}
    .ok{text-decoration:none;}
    /* Au niveau le plus haut du code */
</style>
{# NavBar#}
<nav class="navbar py-0 navbar-expand-sm navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_home') }}">
        <p class="text-info d-inline "style="font-family: 'Poiret One', cursive;margin-right:-8px">W<span class="text-success">A</span><span class="text-white">FORMATION</span></p></a>
        <button class="navbar-toggler navbar-toggler-sm mt-1"; type="button" data-bs-toggle="collapse"data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon" ></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-lg-0">
            <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="{{ path('app_lesson_index') }}">
                    <i class="bi bi-book"></i>
                    <p class="d-none d-md-inline"> Cours</p>
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="{{ path('app_calendar') }}">
                    <i class="bi bi-calendar-date"></i>
                    <p class="d-none d-md-inline"> Calendrier</p>
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="{{ path('app_booking_index') }}">
                    <i class="bi bi-pencil"></i>
                    <p class="d-none d-md-inline"> RDV</p>
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="{{ path('app_invoices') }}">
                    <i class="bi bi-receipt-cutoff"></i>
                    <p class="d-none d-md-inline"> Factures</p>
                </a>
            </li>
            {# <li class="nav-item">
                <a class="nav-link " aria-current="page" href="{{ path('app_invoice_new') }}">Commande</a>
            </li> #}
            </ul>
            <ul class="navbar-nav ms-auto mb-lg-0">
                <li class="nav-item" >
                    <a class="nav-link active" aria-current="page" href="{{ path('cart_show') }}">
                        <i class="bi-cart text-white "></i>
                        <p class="d-none d-md-inline">Pannier</p>
                    </a>
                </li>
            {% if app.user %}
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item" >
                        <a class="nav-link active" aria-current="page" href="{{ path('app_admin') }}">
                            <i class="bi bi-gear-fill"></i>
                        </a>
                    </li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="#">
                        <i class="bi bi-file-earmark-person"></i>
                    </a>
                </li>            
                <li class="nav-item"style="margin-top:2px;margin-left:-5px">
                    <a class="nav-link active" aria-current="page" href="{{ path('app_logout') }}">
                        <i class="bi bi-power"></i>
                    </a>
                </li>
            {% else %}
                <li class="nav-item"style="margin-top:2px">
                    <a class="nav-link active" aria-current="page" href="{{ path('app_login') }}">
                        <i class="bi bi-person"></i>
                        Connexion
                    </a>
                </li>
            {% endif %}
            </ul>
        </div>
    </div>
</nav>
<!-- FOOTER -->
<footer class="scroll text-center bg-dark fixed-bottom">
    <div class="text-center ">
        <a class="text-light text-decoration-none mx-2" href="#"><p style="margin-top:-20px">Conditions générales de vente</p></a>
        <a class="text-light text-decoration-none mx-2" href="#">Partenariats</a>
        <a class="text-success text-decoration-none mx-2 bold" href="#">Contact</a>
    </div>
</footer> 
        {% block body %}{% endblock %}
        {# bootstrap & js #}
        <script src="{{ asset('bootstrap.bundle.min.js')}}" defer>
        </script><script src="{{ asset('js/index.js')}}" defer></script>
        <script src="https://unpkg.com/scrollreveal"></script>
    </body>
</html>